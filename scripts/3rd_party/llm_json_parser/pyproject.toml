[project]
name = "llm-json-parser"
version = "0.1.1"
description = "advanced JSON parser for LLM outputs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.8.15,<0.9.0"]
build-backend = "uv_build"
